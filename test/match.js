// const match = require('../lib/match')

const sample = require('./sample')

describe('Match', () => {
  xit('matches some grantees', function(done) {
    // match.org('focus: hope').then(org => {
    //   org.name.should.equal('Focus: HOPE')
    //   org.id.should.equal(223)
    //   done()
    // })
  })
})
